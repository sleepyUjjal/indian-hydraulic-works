import Hero from "@/components/sections/Hero";

export default function Home() {
  return (
    <main>
      <Hero />

      {/* Future sections will be added below in subsequent commits */}
      {/* Services, Projects, Contact, etc. */}
    </main>
  );
}
